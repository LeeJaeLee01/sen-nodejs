# Exercises | Bài thực hành — Day 27 | Ngày 27

> **Infrastructure as Code (IaC) với Terraform**

## Goal | Mục tiêu

**EN:** Terraform: VPC, ECS cluster, RDS PostgreSQL, ElastiCache Redis for NestJS.

**VI:** Viết một bộ script Terraform cơ bản định nghĩa toàn bộ hạ tầng gồm VPC, ECS Cluster, RDS PostgreSQL và ElastiCache Redis cho app NestJS.

## Steps | Các bước

**EN:**
1. terraform init && terraform plan trong exercises/
2. Modules: vpc, ecs, rds, elasticache (skeleton OK)

**VI:**
1. terraform init && terraform plan trong exercises/
2. Modules: vpc, ecs, rds, elasticache (skeleton OK)

## Deliverable | Sản phẩm nộp

Lưu vào `deliverables/`:

**EN:** terraform plan output (không cần apply prod)

**VI:** terraform plan output (không cần apply prod)

## Checklist

- [ ] Đọc lý thuyết trong `../README.md`
- [ ] Hoàn thành các bước lab
- [ ] Điền `../notes/LEARNINGS.md`
- [ ] Lưu artifact vào `deliverables/`
